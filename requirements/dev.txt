# Includes requirements.txt
-r core.txt

# Pre-commit hooks
pre-commit

# Test dependencies for DATE-SMT
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0  # For parallel test execution
hypothesis>=6.0.0     # For property-based testing
coverage>=7.0.0
